import React from 'react';

import {Gesture} from '../gesture';

import './index.scss';

export function GestureBar() {
    return (
        <section>
            <Gesture/>
            <Gesture/>
            <Gesture/>
        </section>
    );
}
